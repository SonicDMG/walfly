# Walfly does not use a root env file. Next.js loads .env* only from the Next
# project directory and Expo only from the app directory.
#
#   packages/api/.env.local   → all server secrets   (see packages/api/.env.example)
#   apps/mobile/.env          → EXPO_PUBLIC_API_URL  (see apps/mobile/.env.example)
