/**
 * Tab 3 — Global Chat
 * Searches across all recordings using vector similarity.
 */
import ChatScreen from '../../components/ChatScreen';

export default function GlobalChatTab() {
  return <ChatScreen title="Chat with your recordings" />;
}
