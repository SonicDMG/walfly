# Base URL of the Next.js API. `localhost` resolves to the phone itself on a
# physical device — use your machine's LAN IP (and 10.0.2.2 on the Android
# emulator). Expo reads this file; Next.js never will.
EXPO_PUBLIC_API_URL=http://localhost:3000
